#!/bin/bash

ssh -t "$@" 'tmux -L ian attach || tmux -L ian start-server\; set-option -g base-index 1\; set-option -g pane-base-index 1\; set-window-option -g mode-keys vi\; new -d -s default\; send-keys clear C-j\; attach'
