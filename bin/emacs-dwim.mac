#!/bin/bash
# Author: Ian Yang

EMACSCLIENT=/Applications/Emacs.app/Contents/MacOS/bin/emacsclient

if pgrep -u $UID Emacs > /dev/null; then
    if [ -n "$1" ]; then
        $EMACSCLIENT "$@"
    fi
    open -a Emacs
else
    open -a Emacs --args "$@"
fi
