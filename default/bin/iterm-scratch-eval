#!/usr/bin/env bash

set -u
[ -n "${DEBUG:-}" ] && set -x || true

case "${1:-}" in
  --clipboard|--scratch|--vim)
    scratch --eval "$@"
    ;;
  *)
    scratch --eval --vim "$@"
    ;;
esac

# Kill the parent process (the shell) when Vim quits
echo "kill -SIGUSR1 $PPID"
