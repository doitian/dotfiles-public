#!/usr/bin/env bash

set -e
set -u
[ -n "${DEBUG:-}" ] && set -x || true

if [ "$#" = 0 ]; then
  echo "qlint lang [args]" >&2
  exit 1
fi

lang="$1"
shift

case "$lang" in
  *)
    echo "Unknown lang: $lang" >&2
    exit 2
    ;;
esac
