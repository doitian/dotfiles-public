/**
 * Build-time defaults for OpenAI (overridden by build.js when compiling bin).
 * Non-bin usage (e.g. bun run src/hello.js) uses this file as-is.
 */
export const DEFAULT_OPENAI_API_KEY = undefined;
export const DEFAULT_OPENAI_BASE_URL = undefined;
export const DEFAULT_OPENAI_MODEL = undefined;
