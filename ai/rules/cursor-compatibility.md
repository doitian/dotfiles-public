## Tool Mapping

When skills reference tools you don't have, substitute Cursor equivalents:

- `Edit` → `StrReplace`
- `Bash` → `Shell`
- `Skill` tool → Read the skill file with the `Read` tool
- `Read`, `Write`, `TodoWrite`, `Task` → Your native tools (same names)
